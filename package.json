{
  "private": true,
  "scripts": {
    "test:git-worker": "node --test test/git-worker-cacao-chain.test.mjs",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run --outdir dist",
    "witness:git": "node scripts/live-git-cli-witness.mjs"
  },
  "bin": {
    "git-remote-kotobase": "bin/git-remote-kotobase"
  },
  "devDependencies": {
    "@noble/curves": "^2.3.0",
    "@noble/hashes": "^2.0.1",
    "wrangler": "4.103.0"
  }
}
